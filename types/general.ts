// place for custom typescript interfaces/"models"

// delete later
export interface exampleUser {
  authenticated: boolean;
  username: string;
  first_name: string;
  last_name: string;
  email: string;
}
